#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{

    int x = 3;
    int exp = 3;
    
    double result  = pow(x,exp);
    printf("The Power of the x is:%d",result);
    
    return 0;
}